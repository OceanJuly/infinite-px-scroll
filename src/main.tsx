import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import InfinitePxScroll from "./components/infinite-px-scroll";

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <InfinitePxScroll />
  </React.StrictMode>,
)